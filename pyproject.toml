[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
	"pydantic>=2.4.5"
]

[tool.mypy]
plugins = [
  "pydantic.mypy"
]

[dependency-groups]
dev = [
    "mypy>=1.19.1"
]